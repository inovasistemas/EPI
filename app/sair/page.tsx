import Logout from '@/components/Template/Logout'
import { pageMetadata } from '@/lib/misc/metadata'

export const metadata = pageMetadata('Sair')

export default function ProductsPage() {
  return <Logout />
}
