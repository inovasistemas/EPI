import { AccountIcon } from '../components/Display/Icons/Account'

const meta = {
  component: AccountIcon,
}

export default meta

export const OnDark = {
  parameters: {
    backgrounds: {
      default: 'dark',
    },
  },
}
