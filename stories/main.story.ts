import { UserIcon } from '../components/Display/Icons/User'

const meta = {
  component: UserIcon,
}

export default meta

export const OnDark = {
  parameters: {
    backgrounds: {
      default: 'dark',
    },
  },
}
